namespace frankenstein.Server.DTOs.Matrix
{
    public enum ReplyEnum
    {
        OK,
        INVALID_COEFFICIENTS_ERROR,
        INVALID_MATRIX_ERROR,
        NO_DIAGONAL_DOMINANCE,
        ITERATION_LIMIT_EXCEEDED,
        ERROR,
        FILE_NOT_FOUND,
        EMPTY_FILE
    }
}
